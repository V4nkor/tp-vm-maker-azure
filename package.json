{
  "name": "tp-vm-maker-azure",
  "version": "1.0.0",
  "author": "Mathieu Morgat <morgatmathieu@gmail.com>",
  "description": "Azure VM Maker school project repository",
  "private": true,
  "type": "module",
  "files": [
    "dist/",
    "src"
  ],
  "scripts": {
    "test": "echo \"Info: no test specified\"",
    "dev": "pnpm --parallel dev",
    "build": "pnpm --parallel build",
    "build:serve": "pnpm --parallel build:serve",
    "start": "pnpm --parallel start",
    "serve": "pnpm --parallel serve",
    "pm2": "pnpm --parallel pm2",
    "start:pm2": "pm2 start ecosystem.config.cjs",
    "env": "node setup-env.cjs",
    "prepare": "husky",
    "update": "pnpm update --recursive",
    "install:r": "pnpm install --recursive"
  },
  "keywords": [],
  "license": "ISC",
  "engines": {
    "node": ">=20.10.0 <21.0",
    "npm": ">=10.2.4",
    "pnpm": ">=8.14.1"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^19.2.1",
    "@commitlint/config-conventional": "^19.1.0",
    "commitizen": "^4.3.0",
    "cz-conventional-changelog": "^3.3.0",
    "husky": "^9.0.11",
    "pm2": "^5.3.1"
  }
}
